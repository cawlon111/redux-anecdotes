{
  "name": "anecdotes-backend",
  "version": "1.0.0",
  "description": "Backend for anecdotes app",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
